========================================
SSCS MOBILE APP - KEYSTORE INFORMATION
========================================

⚠️  CRITICAL: KEEP THIS FILE SECURE AND PRIVATE ⚠️
Store this information in a password manager or secure vault.
Never commit this file to version control.

========================================
KEYSTORE DETAILS
========================================

Keystore File Location:
  mobile/android/app/upload-keystore.jks

Keystore Password:
  lK/YH0uIccY2J3UCgfFbAFYVsZG00bbP+FT8l3kPtYM=

Key Alias:
  sscs-mobile-key

Key Password:
  lK/YH0uIccY2J3UCgfFbAFYVsZG00bbP+FT8l3kPtYM=
  (Note: Same as keystore password due to PKCS12 format)

========================================
CERTIFICATE DETAILS
========================================

Algorithm: RSA
Key Size: 4096 bits
Validity: 10,000 days (~27 years)
Signature Algorithm: SHA384withRSA

Distinguished Name (DN):
  CN=SSCS Mobile
  OU=Mobile Development
  O=SSCS
  L=Unknown
  ST=Unknown
  C=US

========================================
CONFIGURATION FILES
========================================

1. key.properties (mobile/android/key.properties)
   - Already created and configured
   - Already in .gitignore (won't be committed)

2. build.gradle.kts (mobile/android/app/build.gradle.kts)
   - Already configured with signing config

========================================
BUILD COMMANDS
========================================

Build Release APK:
  cd mobile && flutter build apk --release

Build Release App Bundle (for Play Store):
  cd mobile && flutter build appbundle --release

Build Split APKs (per ABI):
  cd mobile && flutter build apk --split-per-abi --release

========================================
OUTPUT LOCATIONS
========================================

APK: mobile/build/app/outputs/flutter-apk/app-release.apk
App Bundle: mobile/build/app/outputs/bundle/release/app-release.aab

========================================
BACKUP INSTRUCTIONS
========================================

1. Copy the keystore file to a secure backup location:
   - Upload to secure cloud storage (encrypted)
   - Store on external encrypted drive
   - Keep in company password vault

2. If you lose this keystore, you CANNOT update your app on Play Store
   - You would need to publish as a completely new app
   - All existing users would need to uninstall and reinstall

3. Recommended: Create multiple secure backups in different locations

========================================
VERIFICATION
========================================

To verify keystore information:
  keytool -list -v -keystore mobile/android/app/upload-keystore.jks -storepass "lK/YH0uIccY2J3UCgfFbAFYVsZG00bbP+FT8l3kPtYM="

To check certificate fingerprints (needed for Google services):
  keytool -list -v -keystore mobile/android/app/upload-keystore.jks -alias sscs-mobile-key -storepass "lK/YH0uIccY2J3UCgfFbAFYVsZG00bbP+FT8l3kPtYM="

========================================
SECURITY NOTES
========================================

✓ Keystore is already in .gitignore
✓ key.properties is already in .gitignore
✓ Strong 4096-bit RSA encryption
✓ Random 256-bit passwords generated
✓ Valid for 27+ years

⚠️  DO NOT share these passwords
⚠️  DO NOT commit keystore to git
⚠️  DO backup the keystore file securely
⚠️  DO store passwords in a password manager

========================================
Created: 2026-07-22
========================================
